package com.xtu;

public class LC151 {
    public String reverseWords(String s) {
        int slowIndex = 0;  // 慢指针
        int fastIndex = 0;  // 快指针

        while (fastIndex < s.length()) {
            if(s.charAt(fastIndex) == ' '){

            }
        }
        return "";
    }
}
